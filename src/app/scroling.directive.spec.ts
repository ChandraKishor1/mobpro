import { ScrolingDirective } from './scroling.directive';

describe('ScrolingDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrolingDirective();
    expect(directive).toBeTruthy();
  });
});
